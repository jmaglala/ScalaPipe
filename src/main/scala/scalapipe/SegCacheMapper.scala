package scalapipe

private[scalapipe] class SegCacheMapper(
    val _sp: ScalaPipe
) extends Mapper(_sp)
{
    def map() {
        // Ananlyze the graph
        
        // Build the list of segments
    
        // Reassign the buffers
    }
}